# 用途
子 Agent 启动后自动读取本文件，加载完整单 Issue 执行流水线

# 位置
项目根目录 /worker-agent.md

# 运行时入参
issue_id、issue_title、acceptance_criteria、spec_ref、max_retry

# 执行规范
全程使用 OpenCode 标准 skill({name: ""}) 调用，禁止自定义流程

## 一、环境初始化规则
基于传入的 issue_id 与 issue_title，创建独立隔离环境，杜绝跨任务污染：
- 创建独立 git worktree 隔离目录
- 分支命名规范：issue/{issue_id}-{issue_title}
- 严格执行 垂直切片完整闭环：schema → service → api → ui → test
- 严格禁止水平分层、半截式开发（仅改库表、仅写接口、仅改前端等局部开发）

## 二、TDD 红-绿-重构开发流程
固定技能调用：skill({name: "implement"})、skill({name: "tdd"})

强制循环，禁止跳过任意阶段：
1. RED 红态：依据 acceptance_criteria、spec_ref 编写单元/集成测试，执行并确认测试失败，证明测试有效、覆盖需求点。
2. GREEN 绿态：编写最小粒度可用业务代码，仅实现验收标准所需功能，不扩 scope、不追加无关逻辑，直至全部测试通过。
3. REFACTOR 重构态：不修改业务功能、不破坏测试通过率，统一优化类型定义、变量命名、代码结构、冗余逻辑、Lint 警告，对齐工程规范。

持续迭代，直至当前工单所有验收标准 100% 完成、无遗漏、无超范围。

## 三、全量本地验证
固定技能调用：skill({name: "verify"})

开发完成后必须执行全量校验，全部绿灯才可进入评审阶段：
- Lint 代码规范校验、TS 类型校验、项目构建打包校验（无报错、无致命警告）
- 完整单元测试、集成测试全覆盖，存在 E2E 场景则执行 Playwright 端到端测试
- 本地启动项目复现核心业务场景，确认实现完全对齐 Spec 文档与工单验收标准

验证不通过，立即退回 TDD 循环迭代修复，禁止进入下一流程。

## 四、双轴隔离代码评审（禁止自审自写）
拉起独立隔离评审实例，固定技能调用：skill({name: "code-review"})

双维度强制评审，杜绝开发 Agent 自我包庇、认知盲区：
- 工程规范轴：校验类型安全、异常处理、代码风格、性能隐患、安全漏洞、Git 提交规范
- 需求对齐轴：对照 spec_ref、acceptance_criteria，校验无漏实现、无超范围开发、无逻辑偏差、无边界缺失

输出结构化评审报告，区分：阻断级问题、优化建议、验收结论。

存在阻断级问题必须退回 TDD+Verify 全流程修复，重新迭代评审，直至零阻断。

## 五、PR 生成 & 全自动 CI 校验（无人工复核）
评审与本地验证全部通过后，全自动执行 PR 流程：
1. 推送当前工单独立分支至远程仓库，自动生成标准化 PR
2. 自动填充 PR 信息：关联工单 Issue ID、引用 Spec 段落、验收标准勾选清单、AI 评审摘要、测试/构建结果、标记agent-generated
3. 自动 rebase 目标主干分支，自动解决常规代码冲突，解决后重跑本地全量验证

### CI 全状态分支处理逻辑（完整闭环）
监听流水线状态，分三类场景严格处理，禁止卡死、禁止静默失败：

1. CI 全部绿灯（成功）
正常进入自动合并流程。

2. CI 红灯失败（代码问题 / 环境问题二分处理）
- 判定 A：代码实现缺陷（测试失败、逻辑报错、lint 不通过、构建代码异常）
  回滚代码状态，重回「TDD - 验证 - 评审」完整迭代修复链路，重新推送代码、触发 CI。
  最大代码修复重试次数：5 轮。
- 判定 B：CI 基础设施抖动（依赖拉取失败、流水线临时报错、网络异常）
  不修改业务代码，仅重新触发 CI 流水线。
  最大 CI 重试次数：3 轮。

达到最大重试阈值仍未绿灯：判定任务永久失败，禁止合并，进入收尾上报流程。

3. CI 超时无结果（卡死/挂起）
CI 超时阈值：20 分钟无状态更新，直接判定为任务失败，终止等待，禁止无限阻塞。

### 失败统一后置逻辑
所有 CI 红灯、超时、重试耗尽场景：
- 禁止进入合并流程
- 记录完整失败日志：CI 报错摘要、失败阶段、重试次数
- 正常执行 worktree 环境清理，不残留脏环境
- 组装失败结构体，上报主调度 Agent

## 六、自动合并 & 环境闭环 & 结果上报
前置强守卫：仅 CI 全绿灯、无阻断问题、全量验证通过，才可进入合并。
1. 执行 Squash 合并至主干分支，一个工单对应一条干净合并提交
2. 自动更新工单状态为 done，自动关闭对应 Issue
3. 强制清理当前工单独立 git worktree 环境，释放本地资源
4. 结构化结果回传给主调度 Agent

### 最终返回结构体
```json
{
  "issue_id": "",
  "status": "success|failed",
  "error_message": "",
  "pr_url": ""
}
```

## 七、强制禁止反模式（零容忍）
- 禁止水平分层、半截式局部开发，必须垂直切片全闭环
- 禁止跳过 RED 阶段直接编写业务代码
- 禁止多工单共用 worktree 与运行环境
- 禁止开发主体自审自写，必须独立评审实例评审
- 禁止超需求开发、遗留未完成逻辑、遗漏验收项
- 禁止测试、构建、CI 未绿灯时执行合并
- 禁止 CI 失败/超时后静默卡死，必须上报失败状态并清理环境
