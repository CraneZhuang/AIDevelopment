# Copy to .env.local and fill in. The values below are the demo credentials.
ADMIN_ACCOUNT=admin
ADMIN_PASSWORD=123456
SESSION_SECRET=change-me

# Optional. How long a session stays valid, in seconds. Defaults to eight hours, which
# is what the end-to-end suite shortens so it can watch a session expire.
# SESSION_TTL_SECONDS=28800
